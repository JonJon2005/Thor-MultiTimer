ANDROID APP PROJECT CONTEXT REPORT

PROJECT OVERVIEW
- Project name: FirstApp
- Current package name: com.example.firstapp
- Current save location: /Users/jonathangallo/ANDROID_REPOS/firstapp
- This is a Kotlin + Jetpack Compose Android app created in Android Studio.
- The project started as the default Empty Activity template but is now a custom multi-timer app.
- The app is intentionally being built in a beginner-friendly way:
  - Kotlin only
  - Jetpack Compose only
  - Material 3
  - Gradle Kotlin DSL
  - no legacy XML view UI
- The user is learning Android development while building this app, so future work should stay explainable and incremental.

PRIMARY GOAL
- Build a normal Android app intended to be used ONLY on the bottom screen of the AYN Thor.
- Treat the target as a single compact Android screen, not as a dual-screen or foldable app.
- Do NOT introduce:
  - dual-screen APIs
  - spanning
  - foldable APIs
  - second-display-specific architecture
  - Bluetooth controller assumptions
- Design the app around:
  - fast interactions
  - no scroll-heavy navigation
  - touch-friendly controls
  - controller / D-pad support for Thor hardware buttons

CURRENT APP CONCEPT
- The app concept is now clearly defined:
  - It is a multi-timer app.
  - The user can quickly create and manage multiple timers at once.
  - The UI is optimized for a compact device screen and hardware button input.
- The current app is not just a prototype shell anymore.
- It contains a working dashboard, timer logic, settings, theme switching, accent switching, alert behavior, haptics, and hardware controller navigation.

HARDWARE TARGET / SCREEN TARGET
- Target device: AYN Thor bottom screen
- Target usage: bottom screen only
- Do not build for spanning across both displays
- Do not build special multi-display app logic unless explicitly requested later
- Correct bottom-screen resolution assumption:
  - Resolution: 1080 x 1240
  - Orientation assumption: portrait
  - Screen size reference: about 3.92 inches
- Practical layout assumption:
  - This is a compact portrait display with less room than a normal phone
  - Oversized headers and deep vertical stacks can easily break the layout
  - Buttons need to stay large enough to tap reliably
  - The dashboard should avoid hidden content and unnecessary text

CURRENT DEVELOPMENT STACK
- IDE: Android Studio
- Language: Kotlin
- UI toolkit: Jetpack Compose
- Design system base: Material 3
- Build system: Gradle
- Build script format: Kotlin DSL
- App architecture level:
  - single-activity
  - Compose state in MainActivity.kt
  - no ViewModel yet
  - no persistence layer yet
  - no backend
  - no networking
  - no database

CURRENT ANDROID / BUILD CONTEXT
- Minimum SDK: API 29
- Target SDK: 36
- Compile SDK: 36.1
- Java compatibility: 11
- Build features:
  - Compose enabled
- Current known successful build command:
  - ./gradlew :app:assembleDebug
- Current debug APK output path:
  - app/build/outputs/apk/debug/app-debug.apk

CURRENT TESTING CONTEXT
- The app has been tested in:
  - Android emulator
  - real-device style screenshot iteration
  - AYN Thor bottom-screen testing
- A custom emulator profile is intended to approximate the Thor bottom screen:
  - 1080 x 1240
  - portrait
  - no navigation buttons
  - no hardware buttons
- Real-device testing matters because this UI is very size-sensitive and controller-sensitive.
- The Thor may expose the bottom display as an external display, so testing/deployment may require adb display targeting depending on device behavior.

CURRENT APP STATUS
- This is a functional first usable version of the app.
- It is no longer a starter template.
- It includes:
  - working timer creation
  - working timer countdown logic
  - multi-timer dashboard
  - theme switching
  - accent switching
  - vibration feedback
  - finish alert popup
  - system alarm / notification sound fallback
  - controller support using D-pad + face buttons
- Still missing:
  - persistence across app restarts
  - notifications
  - background timer scheduling
  - alarm manager integration
  - foreground service
  - long-term architecture separation
  - tests

CURRENT PROJECT STRUCTURE
Root project contains:
- PROJECT_CONTEXT.txt
- build.gradle.kts
- settings.gradle.kts
- gradle.properties
- gradlew
- gradlew.bat
- app/
- gradle/

App module contains:
- app/build.gradle.kts
- app/proguard-rules.pro
- app/src/main/

Important source files:
- app/src/main/java/com/example/firstapp/MainActivity.kt
- app/src/main/java/com/example/firstapp/ui/theme/Theme.kt
- app/src/main/java/com/example/firstapp/ui/theme/Color.kt
- app/src/main/java/com/example/firstapp/ui/theme/Type.kt
- app/src/main/AndroidManifest.xml
- app/src/main/res/drawable/settings.xml

CURRENT MAINACTIVITY CONTEXT
- MainActivity.kt currently contains most of the application logic and UI.
- MainActivity:
  - extends ComponentActivity
  - calls enableEdgeToEdge()
  - calls setContent { TimerApp() }
- TimerApp:
  - owns main app state
  - applies FirstAppTheme(themeMode, accentColor)
  - manages timers
  - manages dialog visibility
  - manages theme selection
  - manages accent selection
  - manages active finish alerts

CURRENT CORE DATA MODEL
- TimerItem currently contains:
  - id
  - totalSeconds
  - remainingSeconds
  - isRunning
  - endTimeMillis
- Timer state is stored in:
  - mutableStateListOf<TimerItem>()
- Finished timer IDs are tracked separately so the finish alert popup can be shown and cleared safely.

CURRENT TIMER LOGIC
- addTimer(totalSeconds, startImmediately = false):
  - validates totalSeconds > 0
  - inserts the new timer at the front of the list
  - resets currentPage to 0 so the new timer is visible immediately
  - optionally starts immediately by setting endTimeMillis
- updateTimer(timerId, transform):
  - finds the timer by id
  - replaces it with a transformed copy
- Countdown behavior:
  - driven by a LaunchedEffect loop
  - uses System.currentTimeMillis()
  - recomputes remaining seconds from endTimeMillis
  - stops timers when remaining time reaches 0
- Refresh cadence:
  - faster when at least one timer is running
  - slower when idle
- Pause behavior:
  - computes remaining time from endTimeMillis
  - clears running state
  - clears endTimeMillis
- Start behavior:
  - resumes from remainingSeconds if timer is paused
  - restarts from totalSeconds if remainingSeconds == 0
- Reset behavior:
  - restores remainingSeconds to totalSeconds
  - clears running state
  - clears endTimeMillis
- Remove behavior:
  - deletes the timer from the list

CURRENT MAIN SCREEN / DASHBOARD
- The main screen is a fixed dashboard layout.
- It is intentionally not a scrollable timer list.
- Main screen structure:
  - header row
  - quick action row
  - 2x2 timer grid
  - page controls only when needed
- Header:
  - title: Multi Timer
  - settings gear icon on the top right
  - no timer count / running count text
- Quick action row:
  - Custom
  - 5m
  - 10m
  - 15m
- Quick preset behavior:
  - creates timer
  - starts timer immediately
- Custom behavior:
  - opens custom timer dialog
  - created timers do not auto-start
- Page behavior:
  - 4 timers max visible per page
  - page controls appear only if timers exceed 4
  - Previous / Next switch pages

CURRENT TIMER CARD DESIGN
- Timer cards are shown in a 2x2 grid.
- Empty slots remain visible so the grid structure stays stable.
- Active / existing timer cards show:
  - large countdown
  - remove button in the top-right
  - Pause + Reset while running
  - Start or Restart while not running
- Empty slots show:
  - Empty slot
  - Ready for timer
- Controller focus state is visually reflected on timer cards.
- Empty cards are intentionally styled to remain visible even in OLED mode.

CURRENT DIALOGS
Custom timer dialog:
- Opens from Custom button
- Uses compact stepper controls
- Minute controls:
  - decrement by 1
  - increment by 1
- Second controls:
  - decrement by 15
  - increment by 15
  - effectively cycles in usable 15-second increments within the allowed range
- Actions:
  - Cancel
  - Add Timer
- Designed to fit on the compact target screen without cutoff

Settings dialog:
- Opens from top-right gear icon
- Includes real settings now, not a placeholder
- Sections:
  - Theme
  - Accent
  - Close button

Finished timer dialog:
- Appears when a timer reaches zero
- Uses a large custom panel instead of the smaller generic dialog layout
- Takes up a large portion of the screen
- Shows:
  - Timer Finished title
  - oversized Stop Alert button
- Description text was intentionally removed to reduce clutter
- Designed to be controller-friendly and touch-friendly

CURRENT THEME SYSTEM
- The app supports three theme modes:
  - Dark
  - Light
  - OLED
- Dark mode:
  - current standard dark palette
  - dark surfaces and dark background
- Light mode:
  - white / light background
  - same accent-driven style
- OLED mode:
  - pure black background emphasis
  - red/blue/green/purple accent still visible
- Theme changes are applied live in-app.
- Theme choice is currently in-memory / saveable state only.
- Theme choice is not yet persisted across full process death / reinstall.

CURRENT ACCENT COLOR SYSTEM
- The app supports four accent colors:
  - Red
  - Blue
  - Green
  - Purple
- Default accent:
  - Red
- Accent choices are available in the settings dialog.
- Accent selector uses color swatch buttons instead of text labels.
- Selected accent is visually highlighted.
- Accent changes apply live throughout the app.
- Accent choice is not yet persisted beyond current saved UI state scope.

CURRENT VISUAL / UX STYLE
- The app has been repeatedly tuned for the compact Thor bottom screen.
- Current design priorities:
  - no long scrolling experience
  - large tap targets
  - minimal nonessential text
  - strong visual emphasis on active timers
  - quick access to the most common timer actions
- The design has intentionally removed or avoided:
  - timer names on cards
  - header status clutter
  - explanatory copy where it wastes space
  - menus that hide timers unnecessarily

CURRENT AUDIO / ALERT BEHAVIOR
- When a timer finishes:
  - the app attempts to play the device system alarm sound
  - if no default alarm sound exists, it falls back to the system notification sound
- This uses RingtoneManager rather than a bundled sound file.
- Emulator audio may be silent depending on emulator sound configuration.
- Real devices are more reliable for verifying audio behavior.

CURRENT HAPTIC / FEEDBACK BEHAVIOR
- Touch haptics:
  - main shared app buttons vibrate on tap
  - accent swatch buttons vibrate on tap
- Controller haptics:
  - controller A-button confirmation now triggers haptic feedback too
- Button visuals:
  - buttons use a press-scale animation for tap feedback

CURRENT CONTROLLER SUPPORT
- The app now supports hardware D-pad and face-button input for the Thor.
- This is not based on Bluetooth controller support.
- Controller support is handled explicitly in Compose key event processing.
- Supported conceptual inputs:
  - D-pad left
  - D-pad right
  - D-pad up
  - D-pad down
  - primary action (A / center / enter)
  - back action (B)
  - secondary action (X)
  - tertiary action (Y)
- Dashboard controller behavior:
  - D-pad moves focus logically across:
    - settings
    - custom
    - presets
    - timer cards
    - page controls
  - A / primary:
    - activates the focused item
    - starts/pauses timer cards
    - opens empty slot into timer creation flow
  - X / secondary on focused timer card:
    - reset timer
  - Y / tertiary on focused timer card:
    - remove timer
- Dialog controller behavior:
  - D-pad moves between logical controls
  - A confirms the focused control
  - B dismisses the dialog
- Finish alert controller behavior:
  - A stops alert
  - B also stops alert
- Controller focus state is visually highlighted so the user can see what is selected.

IMPORTANT INTERACTION CONSTRAINTS
- The user explicitly does not want behavior tied to Android hardware back/home navigation as the app’s normal flow.
- The app currently does not rely on hardware back/home behavior for user-visible core actions.
- Home cannot be overridden in a normal Android app anyway.
- Dialog dismissal and controller cancellation use explicit app logic.
- The main app should remain operable through on-screen controls and controller input without needing Android navigation buttons.

CURRENT RESOURCE / ICON CONTEXT
- settings.xml in drawable:
  - custom vector drawable used for the settings gear button
  - referenced directly from Compose with painterResource(R.drawable.settings)
- Launcher icon:
  - the manifest currently still points to:
    - @mipmap/ic_launcher
    - @mipmap/ic_launcher_round
  - replacing the app icon is expected to happen through Android Studio Image Asset tooling using a PNG source

CURRENT FILE RESPONSIBILITIES
- MainActivity.kt:
  - app state
  - timer logic
  - dashboard
  - timer cards
  - dialogs
  - controller input handling
  - haptic handling for shared buttons
- Theme.kt:
  - app theme composition
  - theme-mode selection handling at the theme layer
- Color.kt:
  - custom color definitions
  - theme palettes
  - accent color values
- settings.xml:
  - header settings icon drawable

CURRENT LIMITATIONS / KNOWN GAPS
- Timers are still in-memory only.
- If the app process dies, timers are lost.
- Theme and accent choices are not yet persisted into a durable settings store.
- There is no DataStore or SharedPreferences integration yet.
- Timers do not survive reboot or background process loss.
- There is no foreground service or background alarm scheduling.
- There are no Android notifications for finished timers.
- There is no vibration pattern customization yet.
- Controller mapping assumes standard Android key codes; Thor-specific remapping may be needed if the hardware reports unusual codes.
- There is no automated test coverage yet.

SAFE ASSUMPTIONS FOR FUTURE WORK
- Keep using Compose.
- Keep using a single-screen dashboard-first design.
- Avoid introducing scrolling as the main way to manage timers unless the user explicitly changes direction.
- Keep controls large and compact-screen friendly.
- Preserve controller support whenever new features are added.
- Any new dialog or settings UI should be tested for:
  - touch target size
  - controller focus behavior
  - no cutoff on 1080 x 1240 Thor bottom-screen layout

HIGH-VALUE NEXT STEPS IF EXPANDED LATER
- Persist timers and app settings using DataStore
- Add proper app icon assets
- Add notification support for completed timers
- Add optional custom sound asset support
- Add vibration pattern options
- Add user-configurable preset times
- Add labels only if they can be done without harming the compact layout
- Refactor app state out of MainActivity.kt if the code grows much more

BUILD / RUN NOTES
- Standard debug build:
  - ./gradlew :app:assembleDebug
- Android Studio can build an APK from:
  - Build -> Build Bundle(s) / APK(s) -> Build APK(s)
- Debug APK path:
  - app/build/outputs/apk/debug/app-debug.apk

SUMMARY OF CURRENT APP IDENTITY
- This app is now a compact, dark-styled, controller-aware multi-timer dashboard for the AYN Thor bottom screen.
- It prioritizes:
  - speed
  - visibility
  - large touch targets
  - simple timer management
  - low clutter
  - live theme/accent customization
  - explicit interaction through touch and hardware buttons
