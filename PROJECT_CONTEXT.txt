ANDROID APP PROJECT CONTEXT REPORT

PROJECT OVERVIEW
- This is a brand new Android app project.
- Project name: FirstApp
- Current package name: com.example.firstapp
- Current save location: /Users/jonathangallo/ANDROID_REPOS/firstapp
- This is a Kotlin + Jetpack Compose Android app created in Android Studio.
- The user is a complete beginner to Android development, so explanations and changes should be clear, incremental, and beginner-friendly.
- The user wants to learn the modern standard Android stack, not legacy Android patterns unless necessary.

PRIMARY GOAL
- Build a normal Android app that is intended to be used ONLY on the bottom screen of the AYN Thor.
- This app is NOT meant to use dual-screen APIs or dual-screen functionality.
- Treat the target as a normal single-screen Android app with a compact, unusual screen shape.
- The app should be designed around the bottom screen dimensions and usability constraints, not around phone defaults.

HARDWARE TARGET / SCREEN TARGET
- Target usage: AYN Thor bottom screen only
- No dual-screen behavior
- No spanning across screens
- No foldable APIs
- No second-display APIs
- No special multi-window logic unless explicitly requested later
- Working bottom-screen emulation target for development:
  - Screen size: 3.92 inches
  - Resolution: 1240 x 1080
- Design assumption:
  - This is a compact, somewhat squarish screen compared to a normal phone
  - UI should prioritize readability, touch targets, and low clutter
  - Keep layouts simple and efficient

CURRENT DEVELOPMENT STACK
- IDE: Android Studio
- AI coding helper expected: ChatGPT Codex 5.3, likely used via Cursor
- Language: Kotlin
- UI toolkit: Jetpack Compose
- Build configuration language: Kotlin DSL
- Build system: Gradle
- App style: native Android app
- Output target: APK / Android app package

CURRENT ANDROID STUDIO NEW PROJECT SETTINGS
- Template: Empty Activity
- UI approach: Jetpack Compose
- Project name: FirstApp
- Package name: com.example.firstapp
- Save location: /Users/jonathangallo/ANDROID_REPOS/firstapp
- Minimum SDK: API 29 (Android 10.0)
- Build configuration language: Kotlin DSL (build.gradle.kts)

IMPORTANT SDK / VERSION CONTEXT
- The app minSdk is API 29.
- This means the app is allowed to run on Android 10 and above.
- This does NOT mean the emulator must also be API 29.
- Emulator Android version can be higher than minSdk.
- For Thor-like testing, API 33 is preferred if available.
- A higher emulator API is acceptable as long as it is >= 29.

CURRENT TESTING / EMULATOR CONTEXT
- Default installed emulator currently present in Android Studio:
  - Medium Phone API 36.1
  - arm64
  - Android 16.0
- The project has already been run successfully in an emulator.
- A custom AVD is being created specifically to approximate the Thor bottom screen.
- Custom emulator profile being created:
  - Device name: AYN Thor Bottom Screen
  - Device type: Phone/Tablet
  - Screen size: 3.92
  - Resolution: 1240 x 1080
  - Round screen: off
  - RAM: 2048 MB
  - Hardware buttons: off
  - Hardware keyboard: off
  - Navigation style: None
- Preferred emulator system image:
  - Ideally API 33 / Android 13 if available
  - Otherwise a newer available image is acceptable for now

REAL DEVICE TESTING CONTEXT
- The real device target is the AYN Thor.
- The planned real-device workflow is standard Android debugging:
  - Enable Developer Options
  - Enable USB debugging
  - Connect the Thor by USB
  - Select the Thor as the run target in Android Studio
- Physical device testing is planned later for final validation.
- Day-to-day layout testing can use the custom bottom-screen emulator.

CURRENT APP STATUS
- This is a starter project with the default single-activity Compose setup.
- There is no real app functionality yet.
- There is no database yet.
- There is no backend yet.
- There is no networking yet.
- There is no authentication yet.
- There are no custom permissions yet.
- There is no navigation between multiple screens yet.
- There is no business logic yet.
- There is no custom state management beyond the default starter code.
- There is no custom architecture yet beyond the default project skeleton.

CURRENT APP STRUCTURE
Root project contains:
- .gradle
- .idea
- app
- gradle
- build.gradle.kts
- gradle.properties
- gradlew
- gradlew.bat
- local.properties
- settings.gradle.kts

Inside app:
- src
- build.gradle.kts
- proguard-rules.pro

Inside app/src/main:
- java/com/example/firstapp/
- res/
- AndroidManifest.xml

Kotlin source path:
- app/src/main/java/com/example/firstapp/MainActivity.kt

Theme-related files:
- app/src/main/java/com/example/firstapp/ui/theme/...
- These are the default theme files generated by Android Studio for a Compose app.

CURRENT MAINACTIVITY CONTEXT
MainActivity.kt is currently the default Compose starter file structure:
- MainActivity extends ComponentActivity
- onCreate calls setContent
- App theme wrapper is FirstAppTheme
- UI uses Scaffold(modifier = Modifier.fillMaxSize())
- Content calls Greeting(...)
- Greeting(name = "Android", modifier = Modifier.padding(innerPadding)) in the default starter code
- Greeting composable shows a Text("Hello $name!")
- GreetingPreview exists

CURRENT UI STATE
- The app is still basically the default starter Compose UI.
- It is a single text-based starter screen.
- There was emulator output shown during testing that displayed "Hello Test!!" at one point, but the last shared source code snippet still matched the default Greeting pattern with "Android" as the passed name.
- If there is any mismatch between screenshots and source, treat the local source file as the source of truth.

IMPORTANT PROJECT CONSTRAINTS
- Stay on Kotlin.
- Stay on Jetpack Compose.
- Stay on Material 3 / modern Compose patterns unless there is a strong reason not to.
- Do not convert this project to XML Views unless explicitly asked.
- Do not introduce dual-screen APIs.
- Do not assume this app needs multi-display logic.
- Do not over-engineer early.
- Keep the app single-purpose and simple unless new requirements are given.
- Prefer small, explainable changes over large framework-heavy rewrites.
- Preserve the existing project setup unless there is a clear reason to change it.

BUILD / GRADLE CONTEXT
- Gradle is the Android build system for this project.
- Build scripts use Kotlin DSL, not Groovy.
- Relevant build files:
  - Root build file: build.gradle.kts
  - App module build file: app/build.gradle.kts
  - settings.gradle.kts defines project/module structure
  - gradle.properties contains build-wide settings
  - local.properties contains machine-local SDK path
- Any changes to dependencies, Compose settings, namespace, sdk versions, etc. should be made in the correct Gradle Kotlin DSL files.
- Do not convert build scripts to Groovy.
- Keep Gradle changes minimal and explain why each change is needed.

BEGINNER SUPPORT CONTEXT
- The user is new to Android development.
- The user benefits from:
  - line-by-line explanations
  - file-by-file explanations
  - plain-English rationale
  - small changes first
  - telling them exactly where files are
  - avoiding too much Android jargon without explanation
- When suggesting changes, explain:
  - what file to open
  - what to replace
  - why the change matters
  - what result the user should expect

DESIGN / UX GUIDELINES FOR THIS APP
- Optimize for a compact screen.
- Keep touch targets reasonably large.
- Avoid dense, cluttered layouts.
- Avoid wasting too much screen area on oversized top bars or decorative UI.
- Favor simple layouts:
  - Column
  - Row
  - Box
  - LazyColumn
- Prefer readable text sizes and obvious spacing.
- Use responsive Compose layouts rather than hardcoding assumptions for a normal tall phone.
- Test visually against the custom 1240 x 1080, 3.92-inch emulator profile.

CODING PREFERENCES FOR FUTURE WORK
- Use idiomatic Kotlin.
- Use Compose for UI.
- Keep logic simple and readable.
- Prefer a single-activity Compose app unless requirements change.
- Add features incrementally.
- Avoid unnecessary libraries.
- Avoid architectural complexity too early.
- If adding state, start simple with remember / mutableStateOf where appropriate, then introduce ViewModel only when useful.
- If adding navigation later, use Navigation Compose.
- If adding persistence later, choose a simple, modern Android-friendly approach.
- Keep names clean and beginner-readable.

FILES MOST LIKELY TO MATTER EARLY
- app/src/main/java/com/example/firstapp/MainActivity.kt
- app/src/main/java/com/example/firstapp/ui/theme/*
- app/src/main/AndroidManifest.xml
- app/build.gradle.kts
- app/src/main/res/*

SAFE DEFAULT ASSUMPTIONS FOR CODEX
- The user wants a modern native Android app, not a cross-platform framework.
- The user wants Kotlin + Jetpack Compose only.
- The user is learning, so generated code should be understandable.
- The app should work as a normal single-screen Android app.
- The target layout should fit the Thor bottom-screen emulator profile.
- The app should remain compatible with the current project setup unless explicitly changed.
- Prefer API-safe code compatible with minSdk 29.
- Do not assume the app should be Thor-exclusive at the code level unless requested.
- Do not assume landscape-only or portrait-only unless the user explicitly decides that later.
- Do not assume the app needs permissions, network access, storage, or sensors unless requested.

CURRENT KNOWN OPEN QUESTIONS
- The actual app concept/functionality beyond the starter project has not been finalized in this context.
- Final desired orientation behavior has not been explicitly locked down.
- Final navigation structure is unknown.
- Final data model is unknown.
- Final app branding/name may change later.
- Real-device testing on the physical Thor is planned but not yet the main workflow.
- The custom emulator is being used to approximate the bottom screen, not to emulate full Thor hardware behavior.

WHAT CODEX SHOULD AVOID
- Do not introduce XML layouts.
- Do not replace Compose with Views.
- Do not create dual-screen features.
- Do not assume a phone-standard tall layout is acceptable without checking compact-screen usability.
- Do not make major Gradle changes without explaining them.
- Do not rename package/project/files unless asked.
- Do not add lots of dependencies unless clearly needed.
- Do not jump straight into advanced architecture unless the project grows enough to justify it.

SHORT SUMMARY
This is a fresh Android Studio Kotlin + Jetpack Compose project named FirstApp with package com.example.firstapp and minSdk 29. It is intended to be a normal Android app used only on the bottom screen of the AYN Thor, with no dual-screen behavior at all. Development should stay modern, native, and beginner-friendly. The key special requirement is that UI should be designed and tested against a compact 3.92-inch 1240 x 1080 screen profile. A custom emulator profile is being created for that screen, while real-device Thor testing will happen later via USB debugging.